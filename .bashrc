#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto't
PS1='[\u@\h \W]\$ '

fastfetch --logo /home/user/Rice/Images/makima_edit.png --logo-type kitty  --logo-width 30
